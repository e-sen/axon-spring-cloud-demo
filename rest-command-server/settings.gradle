pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
rootProject.name = 'rest-command-server'

include 'common'

project(':common').projectDir = new File(settingsDir, '../common')